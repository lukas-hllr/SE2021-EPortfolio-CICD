import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.eportfolio.cicd',
  appName: 'E-Portfolio-CI-CD',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
